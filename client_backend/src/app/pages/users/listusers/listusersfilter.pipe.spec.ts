import { ListusersfilterPipe } from './listusersfilter.pipe';

describe('ListusersfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ListusersfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
